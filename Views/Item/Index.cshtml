@model PaintERP.Models.ViewModels.ItemListViewModel
@{
    ViewData["Title"] = "Item Master";
    ViewData["Action"] = "Index";
}

<section class="item-module">
    <div class="item-header">
        <div>
            <h1>Item Master</h1>
            <p>Manage inventory items, SKUs, pricing, and warehouse stock levels across all facilities.</p>
        </div>
        <a asp-action="Create" class="primary-btn">
            <span>+</span> Add New Item
        </a>
    </div>

    <div class="item-stats">
        <div class="stat-card">
            <span>Total Items</span>
            <strong>@Model.TotalCount</strong>
            <small>Active SKUs</small>
        </div>
        <div class="stat-card">
            <span>Inventory Value</span>
            <strong>@Model.TotalInventoryValue.ToString("C")</strong>
            <small>Total assets</small>
        </div>
        <div class="stat-card">
            <span>Low Stock Alerts</span>
            <strong>@Model.LowStockCount</strong>
            <small>Below reorder point</small>
        </div>
    </div>

    <div class="item-toolbar">
        <form asp-action="Index" method="get" class="filters">
            <input type="search" name="searchTerm" placeholder="Search SKU, name, barcode..." value="@Model.SearchTerm" />
            <select name="filterType" onchange="this.form.submit()">
                <option value="All" selected="@(Model.FilterType == "All")">All Types</option>
                <option value="Inventory" selected="@(Model.FilterType == "Inventory")">Inventory</option>
                <option value="Non Inventory" selected="@(Model.FilterType == "Non Inventory")">Non Inventory</option>
                <option value="Service" selected="@(Model.FilterType == "Service")">Service</option>
                <option value="Assembly" selected="@(Model.FilterType == "Assembly")">Assembly</option>
                <option value="Raw Material" selected="@(Model.FilterType == "Raw Material")">Raw Material</option>
                <option value="Finished Product" selected="@(Model.FilterType == "Finished Product")">Finished Product</option>
            </select>
            <select name="filterCategory" onchange="this.form.submit()">
                <option value="All" selected="@(Model.FilterCategory == "All")">All Categories</option>
            </select>
            <select name="filterSource" onchange="this.form.submit()">
                <option value="All" selected="@(Model.FilterSource == "All")">All Sources</option>
                <option value="Production" selected="@(Model.FilterSource == "Production")">From Paint Production</option>
                <option value="NonProduction" selected="@(Model.FilterSource == "NonProduction")">Not From Production</option>
            </select>
        </form>
        <button class="ghost">Export</button>
    </div>

    <div class="item-table-card">
        <table class="item-table">
            <thead>
                <tr>
                    <th>Item</th>
                    <th>Type</th>
                    <th>Category</th>
                    <th>Warehouse</th>
                    <th>Current Stock</th>
                    <th>Available</th>
                    <th>Inventory Value</th>
                    <th>Selling Price</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Items)
                {
                    var stockClass = item.CurrentStock <= 0 ? "stock-critical" : item.CurrentStock < 50 ? "stock-low" : "stock-good";
                    <tr>
                        <td>
                            <div class="item-cell">
                                <div class="item-color" style="background-color: @item.ColorHex;"></div>
                                <div class="item-info">
                                    <span class="item-sku">@item.SKU</span>
                                    <span class="item-name">@item.Name</span>
                                    <small class="item-upc">@item.UPCBarcode</small>
                                </div>
                                @if (item.IsHazardousMaterial)
                                {
                                    <span class="hazard-badge">⚠️ Hazardous</span>
                                }
                            </div>
                        </td>
                        <td>
                            <span class="item-type">@item.ItemType</span>
                            <small>@item.Brand</small>
                        </td>
                        <td>
                            <span>@item.Category</span>
                        </td>
                        <td>
                            <span>@item.WarehouseName</span>
                        </td>
                        <td>
                            <span class="stock @stockClass">@item.CurrentStock.ToString("0.##") @item.UnitOfMeasure</span>
                        </td>
                        <td>
                            <span class="stock">@item.AvailableStock.ToString("0.##") @item.UnitOfMeasure</span>
                        </td>
                        <td>
                            <span class="amount">@item.InventoryValue.ToString("C")</span>
                        </td>
                        <td>
                            <span class="amount">@item.SellingPrice.ToString("C")</span>
                        </td>
                        <td>
                            <div class="actions">
                                <a asp-action="Edit" asp-route-id="@item.Id" class="action-btn" title="Edit">✏️</a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="action-btn danger" title="Delete">🗑️</a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <partial name="_Pagination" model="Model.Items" />
</section>

@{
    ViewData["searchTerm"] = Model.SearchTerm;
    ViewData["filterType"] = Model.FilterType;
    ViewData["filterCategory"] = Model.FilterCategory;
    ViewData["filterSource"] = Model.FilterSource;
}
